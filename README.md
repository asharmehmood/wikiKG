# wikiKG
QA system for Wikipedia URLs that extracts, indexes, and answers questions from article content using LLMs, embeddings, and retrieval-augmented generation (RAG). Supports URL ingestion, semantic search, contextual question answering, and scalable document processing pipelines.
