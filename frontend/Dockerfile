# Standalone frontend dev server (not used in production compose).
# In production the frontend is built into the backend Docker image.
FROM node:20-alpine
WORKDIR /app
# TODO T-27: COPY package*.json ./, RUN npm ci, COPY src, EXPOSE 5173, CMD vite
